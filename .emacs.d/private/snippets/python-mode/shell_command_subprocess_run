# -*- mode: snippet -*-
#name : shell_command_subprocess_run
#key : shell_command_subprocess_run
#contributor : Ubuntu
# --
import subprocess

# Example using a pipe with shell=True
result = subprocess.run(
    "ls", 
    shell=True, 
    capture_output=True, 
    text=True
)

print(f"result: {result.stdout.strip()}")
